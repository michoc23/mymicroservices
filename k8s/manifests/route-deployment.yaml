apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: route-service
  template:
    metadata:
      labels:
        app: route-service
    spec:
      containers:
        - name: route-service
          image: docker.io/your-organization/route-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: transport-config
            - secretRef:
                name: transport-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 15
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: route-service
spec:
  selector:
    app: route-service
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
